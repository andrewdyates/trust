# trust-type-recover: Multi-strategy type recovery for reverse compilation
#
# Recovers high-level types from binary analysis artifacts (DWARF info,
# value-set analysis, access patterns). Part of the reverse compilation
# pipeline (v6 Phase VII, step G3).
#
# Author: Andrew Yates <andrew@andrewdyates.com>
# Copyright 2026 Andrew Yates | License: Apache 2.0

[package]
name = "trust-type-recover"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Multi-strategy type recovery from binary analysis artifacts for reverse compilation"

[dependencies]
trust-types = { workspace = true }
thiserror = { workspace = true }
